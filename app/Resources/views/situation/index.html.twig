{% extends 'base.html.twig' %}

{% block body %}
    <h1>Situations list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Finance</th>
                <th>Langue</th>
                <th>Informatique</th>
                <th>Motivation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for situation in situations %}
            <tr>
                <td><a href="{{ path('situation_show', { 'id': situation.id }) }}">{{ situation.id }}</a></td>
                <td>{{ situation.type }}</td>
                <td>{{ situation.finance }}</td>
                <td>{{ situation.langue }}</td>
                <td>{{ situation.informatique }}</td>
                <td>{{ situation.motivation }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('situation_show', { 'id': situation.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('situation_edit', { 'id': situation.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('situation_new') }}">Create a new situation</a>
        </li>
    </ul>
{% endblock %}
